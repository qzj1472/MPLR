<vio:ContentDialog x:Class="MPLR.Views.AddRoomContentDialog"
                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                   xmlns:local="clr-namespace:MPLR.Views"
                   xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                   xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                   xmlns:vio="http://schemas.lepo.co/wpfui/2022/xaml/violeta"
                   Title="{I18N AddRoom}"
                   d:DesignHeight="NaN"
                   d:DesignWidth="NaN"
                   CloseButtonText=""
                   DefaultButton="Primary"
                   FontSize="14"
                   PrimaryButtonClick="OnPrimaryButtonClick"
                   PrimaryButtonText="{I18N ButtonOfAdd}"
                   SecondaryButtonText="{I18N ButtonOfClose}"
                   mc:Ignorable="d">
    <ui:Grid>
        <ui:StackPanel MinWidth="525" Spacing="8">
            <TextBlock Text="{I18N EnterRoomUrl}" />
            <ui:TextBox PlaceholderText="https://live.douyin.com/xxx" Text="{Binding Url, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            <ui:Grid ColumnDefinitions="Auto,Auto,Auto">
                <CheckBox Grid.Column="0" Content="{I18N AddRoomForce}" IsChecked="{Binding IsForcedAdd, Mode=TwoWay}" />
                <CheckBox Grid.Column="1" Margin="18,0,0,0" Content="{I18N FollowGlobalSettings}" IsChecked="{Binding IsFollowGlobalSettings, Mode=TwoWay}" />
                <CheckBox Grid.Column="2" Margin="18,0,0,0" Content="{I18N LiveNotification}" IsChecked="{Binding IsToNotify, Mode=TwoWay}" />
            </ui:Grid>
        </ui:StackPanel>
    </ui:Grid>
</vio:ContentDialog>

